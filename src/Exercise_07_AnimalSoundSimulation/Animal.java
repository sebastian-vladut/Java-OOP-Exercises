package Exercise_07_AnimalSoundSimulation;

/**
 * An interface defining a contract for all animal types.
 * Any class that implements this interface must provide a function makeSound().
 */
public interface Animal {
    public String makeSound();
}
